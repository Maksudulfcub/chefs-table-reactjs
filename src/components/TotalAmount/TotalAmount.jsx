const TotalAmount = ({ totalTime,  }) => {
    return (
        <div>
            <div className="text-xs font-semibold">
                <p>Total time :{totalTime}</p>
            </div>
        </div>
    );
};

export default TotalAmount;